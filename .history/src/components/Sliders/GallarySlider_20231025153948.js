import React, { useEffect, useRef, useState } from "react";
import React from 'react'
import Slider from "react-slick";

const GallarySlider = () => {
    const sliderRef = useRef(null);

  return (
    <div>GallarySlider</div>
  )
}

export default GallarySlider